<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
  <a class="navbar-brand" href="#">BioKey</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-default" aria-controls="nav-default" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="nav-default">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        {{#link-to 'index' class='nav-link'}}Home{{/link-to}}
      </li>
      {{#if session.isAuthenticated}}
      <li class="nav-item">
        {{#link-to 'users' class='nav-link'}}Users{{/link-to}}
      </li>
      <li class="nav-item">
        {{#link-to 'machines' class='nav-link'}}Machines{{/link-to}}
      </li>
      <li class="nav-item">
        {{#link-to 'organizations' class='nav-link'}}Organizations{{/link-to}}
      </li>
      {{/if}}
    </ul>
    <ul class="navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
      <a href="#" class="nav-item nav-link disabled">Hi, {{currentUser.user.name}}</a>
      <a href="#" class="nav-item nav-link" {{action 'logout'}}>Logout</a>
      {{else}}
      {{#link-to 'login' class='nav-item nav-link'}}Login{{/link-to}}
      <a class="nav-item nav-link disabled">or</a>
      {{#link-to 'register' class='nav-item nav-link'}}Register{{/link-to}}
      {{/if}}
    </ul>
  </div>
</nav>